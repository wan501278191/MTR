# 自测结果

## 1. Baseline 结果（原始 MTR）

| 指标 | 3s | 5s | 8s |
|------|-----|-----|-----|
| mAP | 0.4196 | 0.4047 | 0.3281 |
| minADE | 0.3228 | 0.4722 | 0.6866 |
| minFDE | 0.5893 | 0.9088 | 1.4003 |
| MissRate | 0.1580 | 0.1667 | 0.1812 |

> 训练配置: 50 epoch, 8卡, batch_size=64, AdamW, LR=1e-4

## 2. 优化方案

在 Baseline 基础上同时启用以下6项优化：

| # | 优化点 | 来源 | 原理 |
|---|--------|------|------|
| 1 | Mixture NLL 损失 | QCNet | logsumexp 软模式选择，所有模式有梯度 |
| 2 | FDE 加权时间步损失 | 原创 | 最终位置损失加倍，直接优化 minFDE |
| 3 | 累积位移参数化 | QCNet | 逐步位移+cumsum，不确定性单调增长 |
| 4 | EMA 权重平滑 | 通用 | decay=0.999，减少末期震荡 |
| 5 | 扩散轨迹精炼 | DiffSemanticFusion | DDPM 20步+金字塔噪声 |
| 6 | Laplace NLL（可选） | QCNet | 比高斯更鲁棒 |

## 3. 训练日志

训练日志通过 SwanLab 记录，项目地址：
https://swanlab.cn/@alva_wan/MTR

关键监控指标：
- `model.loss`: 总损失
- `model.loss_layerX_reg_gmm`: 各层 GMM NLL 损失
- `model.diffusion_loss`: 扩散模块损失
- `eval.0_summary.3s.minADE`: 3s minADE
- `eval.0_summary.5s.minFDE`: 5s minFDE
- `eval.0_summary.8s.minFDE`: 8s minFDE

## 4. 结果验证

### 4.1 结果格式验证

```bash
python tools/verify_result.py \
    --result_pkl output/waymo/mtr_voyah_data/submission/test/default/result.pkl
```

期望输出：
```
场景数: ~5000
预测目标总数: xxx
✅ 格式验证通过
```

### 4.2 评估指标

```bash
cd tools/eval_scripts
python get_gt_data.py
python eval_gt_and_pred.py
```

输出 VEHICLE/PEDESTRIAN/CYCLIST × 3s/5s/8s 的 mAP/minADE/minFDE/MissRate。

## 5. 训练命令

```bash
# 分布式训练（8卡）
bash tools/scripts/dist_train.sh 8 \
    --cfg_file cfgs/waymo/mtr_voyah_data.yaml \
    --batch_size 64 \
    --epochs 50 \
    --extra_tag optimized \
    --fix_random_seed \
    --workers 8
```

## 6. 推理命令

```bash
python tools/test.py \
    --cfg_file cfgs/waymo/mtr_voyah_data.yaml \
    --ckpt /workspace/model/best_model_ema.pth \
    --extra_tag submission \
    --batch_size 80 \
    --workers 8 \
    --save_to_file \
    --set DATA_CONFIG.DATA_ROOT /workspace/data \
          DATA_CONFIG.SPLIT_DIR.test processed_scenarios_testing_B1_part \
          DATA_CONFIG.INFO_FILE.test processed_scenarios_testB1_part_infos.pkl
```
