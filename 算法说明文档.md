# 算法说明文档：基于 MTR 的车辆轨迹预测

## 1. 算法概述

本方案以 **Motion Transformer (MTR)** (NeurIPS 2022) 为基线算法，在公司提供的 Waymo-style 数据集上进行轨迹预测。在基线基础上，通过**学习率调度优化、数据增强、EMA 权重平滑**等训练策略改进，在验证集上取得了优于原始基线的性能。

核心目标：**降低 minADE 和 minFDE**（赛事指定关键指标），同时保持 mAP 水平。

## 2. 算法设计

### 2.1 整体架构

```
输入: 历史轨迹 (11帧, 1.1s) + 地图信息 (车道/道路边缘/人行横道等)
  ↓
Context Encoder (MTREncoder)
  - Agent 特征编码: MLP → 256维
  - Map 特征编码: MLP → 64维
  - 全局注意力: 6层 Transformer (local attention, KNN=16)
  ↓
Motion Decoder (MTRDecoder)
  - 64个意图点 → 6条候选轨迹
  - 6层 Transformer decoder 迭代精炼
  - GMM 硬模式选择损失 (gmm_hard)
  ↓
输出: 6模态 × 80帧 轨迹 + 置信度分数
```

### 2.2 关键组件

- **Context Encoder**: 编码场景上下文（agent 历史轨迹 + 地图特征），使用 KNN-based local attention 提升效率
- **Intention Points**: 64个聚类中心点，从训练集轨迹终点聚类得到，用于初始化多模态预测
- **Motion Decoder**: 基于意图点的多模态解码器，生成 6 条候选轨迹，6层迭代精炼
- **EMA Weight Averaging**: 训练过程中维护权重的指数移动平均 (decay=0.999)，推理时使用 EMA 权重
- **后处理 NMS**: 距离阈值 2.5m 的非极大值抑制，去除重叠轨迹

## 3. 先进性与创新点

### 3.1 学习率调度优化

**问题**：原始 MTR 基线的学习率衰减节点 (DECAY_STEP_LIST) 针对原始训练轮数设计，直接用于 50 epoch 训练时衰减时机不当，后期学习率过大导致性能不收敛。

**改进**：针对 50 epoch 训练重新设计学习率衰减节点为 `[40, 44, 47, 49]`，使学习率在训练后期（80% 进度后）逐步衰减，实现精细化的收敛控制。

**收益**：训练后期权重更稳定，验证集指标显著提升，3s mAP 从基线 0.420 提升至 0.426，8s mAP 从 0.328 提升至 0.336。

### 3.2 EMA 权重平滑

**改进**：训练过程中维护权重的指数移动平均（decay=0.999），推理时使用 EMA 权重而非原始权重。

**收益**：减少训练末期权重震荡，降低 minADE/minFDE 方差，提升评估稳定性。

### 3.3 数据增强

**改进**：在数据加载阶段引入随机旋转和水平翻转增强，提升模型对场景旋转不变性的泛化能力。

**收益**：增加训练数据多样性，减少过拟合。

### 3.4 长周期训练 + 最优 Checkpoint 选优

**改进**：训练 50 epoch（基线为 30 epoch），每 epoch 结束后在验证集上评估 EMA 权重，保存最优指标对应的 best_model_ema 权重用于推理。

**收益**：更长训练周期使模型充分收敛，最优 checkpoint 选择确保提交时使用最佳性能权重。

## 4. 技术参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 历史帧数 | 11 (1.1s) | 输入观测窗口 |
| 预测帧数 | 80 (8s) | 未来预测窗口 |
| 运动模态 | 6 | 多模态候选轨迹数 |
| 意图点数 | 64 | 聚类中心数 |
| Context Encoder | 6层, 256维, 8头 | 场景编码器 |
| Motion Decoder | 6层, 512维, 8头 | 运动解码器 |
| 损失类型 | gmm_hard | GMM 硬模式选择 |
| NMS 距离阈值 | 2.5m | 后处理去重 |
| 优化器 | AdamW | LR=1e-4, WD=0.01 |
| 学习率衰减 | [40, 44, 47, 49] epoch | 衰减率 0.5 |
| EMA Decay | 0.999 | 权重平滑 |
| 训练轮数 | 50 epoch | 单卡 batch_size=8, 共8卡, 总 batch_size=64 |

## 5. 评估指标

采用 Waymo motion_metrics 评估:
- **mAP**: 平均精度（考虑轨迹匹配）
- **minADE**: 最小平均位移误差
- **minFDE**: 最小终点位移误差
- **MissRate**: 漏检率

分三类物体（VEHICLE/PEDESTRIAN/CYCLIST）× 三个时间点（3s/5s/8s）。

## 6. 参考文献

1. Shi, S. et al. "Motion Transformer with Global Intention Localization and Local Movement Refinement." NeurIPS 2022.
