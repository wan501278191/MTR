# 算法说明文档：基于 MTR 的车辆轨迹预测（融合 QCNet + DiffSemanticFusion 优化）

## 1. 算法概述

本方案以 **Motion Transformer (MTR)** (NeurIPS 2022) 为基线算法，融合 **QCNet** 的 query-centric 设计理念和 **DiffSemanticFusion** 的扩散去噪技术，在公司提供的 Waymo-style 数据集上进行轨迹预测优化。

核心目标：**降低 minADE 和 minFDE**（赛事指定关键指标），同时保持 mAP 水平。

## 2. 算法设计

### 2.1 整体架构

```
输入: 历史轨迹 (11帧, 1.1s) + 地图信息 (车道/道路边缘/人行横道等)
  ↓
Context Encoder (MTREncoder)
  - Agent 特征编码: MLP → 256维
  - Map 特征编码: MLP → 64维
  - 全局注意力: 6层 Transformer (local attention, KNN=16)
  ↓
Motion Decoder (MTRDecoder + 创新优化)
  - 64个意图点 → 6条候选轨迹
  - 6层 Transformer decoder + 累积位移参数化
  - Mixture NLL 损失 + FDE 时间步加权
  ↓
Diffusion Trajectory Refiner (扩散精炼)
  - DDPM 20步 cosine schedule
  - 金字塔多分辨率噪声
  - 以确定性预测为条件的精炼
  ↓
输出: 6模态 × 80帧 轨迹 + 置信度分数
```

### 2.2 关键组件

- **Context Encoder**: 编码场景上下文（agent 历史轨迹 + 地图特征），使用 KNN-based local attention 提升效率
- **Intention Points**: 64个聚类中心点，从训练集轨迹终点聚类得到，用于初始化多模态预测
- **Motion Decoder**: 基于意图点的多模态解码器，生成 6 条候选轨迹，6层迭代精炼
- **Diffusion Refiner**: 基于DDPM的轨迹精炼模块，对确定性预测进行扩散采样精炼
- **EMA Weight Averaging**: 训练过程中维护权重的指数移动平均，减少末期震荡
- **后处理 NMS**: 距离阈值 2.5m 的非极大值抑制，去除重叠轨迹

## 3. 先进性与创新点

### 3.1 软模式选择的 Mixture NLL 损失（借鉴 QCNet）

**问题**：原始 MTR 使用 `argmin` 硬选最近意图点为正样本，梯度只流向一个模式，其他模式无学习信号。

**创新**：借鉴 QCNet 的 MixtureNLLLoss，用 `logsumexp` 实现软模式选择：

```python
# 所有模式都有梯度信号
nll_per_mode = compute_nll_per_mode(pred_trajs, gt_trajs)  # (N, num_modes)
log_pi = F.log_softmax(pred_scores, dim=-1)
loss = -torch.logsumexp(log_pi - nll_per_mode, dim=-1)  # soft min over modes
```

**收益**：所有候选轨迹模式都有梯度信号，提升多模态覆盖率，直接改善 minFDE。

### 3.2 累积位移轨迹参数化（借鉴 QCNet）

**问题**：MTR 直接回归80帧绝对位置，远端帧（8s）误差累积大。

**创新**：借鉴 QCNet，预测逐步位移再 cumsum 还原位置：

```python
pred_positions = torch.cumsum(per_step_delta, dim=2)  # 逐步位移累积
scale = torch.cumsum(F.softplus(scale_raw), dim=2)    # 不确定性单调增长
```

**收益**：每步只需预测小 delta，不确定性随时间单调增长（符合物理直觉），改善远端（8s）预测精度。

### 3.3 金字塔噪声扩散轨迹精炼（借鉴 DiffSemanticFusion）

**问题**：MTR 的6条轨迹由确定性 decoder 生成，多样性不足。

**创新**：借鉴 DiffSemanticFusion，用 DDPM 对 MTR 预测进行扩散精炼：

- **金字塔多分辨率噪声**：代替 i.i.d. 高斯，产生时间相关扰动
- **Cosine beta schedule**：20步快速推理
- **观测条件精炼**：以 MTR 确定性预测为起点，部分去噪
- **FiLM 条件机制**：时间步嵌入通过逐通道缩放/偏移注入

```python
# 金字塔噪声：多尺度时间相关噪声
def pyramid_noise_like(x, discount=0.9):
    for i in range(4):
        coarse = torch.randn(b, c, t // (2**i))
        noise += F.interpolate(coarse, size=t) * (discount ** i)
    return noise / noise.std()
```

**收益**：扩散采样增加轨迹终点覆盖度，直接降低 minFDE。

### 3.4 EMA 权重平滑 + 多 Checkpoint 选优

**创新**：训练过程中维护权重的指数移动平均（decay=0.999），推理时使用 EMA 权重。

**收益**：减少训练末期权重震荡，直接降低 minADE/minFDE 方差。

### 3.5 FDE 加权时间步损失

**创新**：对最终位置时间步（第80帧）损失权重加倍，直接优化 minFDE 指标。

### 3.6 Laplace NLL 损失（借鉴 QCNet）

**创新**：提供 Laplace 分布 NLL 作为可选损失，比高斯 GMM 对异常值更鲁棒。

## 4. 技术参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 历史帧数 | 11 (1.1s) | 输入观测窗口 |
| 预测帧数 | 80 (8s) | 未来预测窗口 |
| 运动模态 | 6 | 多模态候选轨迹数 |
| 意图点数 | 64 | 聚类中心数 |
| Context Encoder | 6层, 256维, 8头 | 场景编码器 |
| Motion Decoder | 6层, 512维, 8头 | 运动解码器 |
| 地图线段数 | 768 (context) / 256+128 (decoder) | 地图特征数 |
| NMS 距离阈值 | 2.5m | 后处理去重 |
| 优化器 | AdamW | LR=1e-4, WD=0.01 |
| 学习率衰减 | [22, 24, 26, 28] epoch | 衰减率 0.5 |
| EMA Decay | 0.999 | 权重平滑 |
| 扩散步数 | 20 | DDPM cosine schedule |
| 损失类型 | gmm_mixture | 软模式选择 |

## 5. 评估指标

采用 Waymo motion_metrics 评估:
- **mAP**: 平均精度（考虑轨迹匹配）
- **minADE**: 最小平均位移误差
- **minFDE**: 最小终点位移误差
- **MissRate**: 漏检率

分三类物体（VEHICLE/PEDESTRIAN/CYCLIST）× 三个时间点（3s/5s/8s）。

## 6. 参考文献

1. Shi, S. et al. "Motion Transformer with Global Intention Localization and Local Movement Refinement." NeurIPS 2022.
2. Zhou, Z. et al. "Query-Centric Trajectory Prediction." CVPR 2023.
3. DiffSemanticFusion: Diffusion-based Semantic Fusion for Mapless Trajectory Prediction.
4. Ho, J. et al. "Denoising Diffusion Probabilistic Models." NeurIPS 2020.
