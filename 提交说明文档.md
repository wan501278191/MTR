# MTR 轨迹预测 — 赛事提交说明文档

## 一、赛题信息

| 项目 | 内容 |
|------|------|
| 赛题名称 | 目标车辆轨迹预测算法 |
| 赛道 | B赛道 |
| 关键指标 | 平均位移误差 (minADE)、最终位移误差 (minFDE)、mAP |

## 二、提交内容清单

| 文件 | 说明 |
|------|------|
| `docker镜像.tar` | 完整可运行的 Docker 镜像 Tar 包（唯一复现源） |
| `运行说明文档.md` | 一行指令对应一个需求的操作说明 |
| `算法说明文档.md` | 算法设计、先进性、创新点 |
| `自测结果.md` | 训练日志、自测指标 |

## 三、Docker 镜像使用说明

### 3.1 镜像加载

```bash
# 加载 Docker 镜像
docker load -i mtr_voyah_submission.tar

# 查看镜像
docker images | grep mtr
```

### 3.2 目录结构（挂载方式）

镜像内置了代码和环境，数据和模型通过 volume 挂载：

```
/workspace/
├── MTR/                    # 算法代码（镜像内置）
│   ├── tools/              # 训练/推理/评估脚本
│   ├── mtr/                # 模型代码
│   ├── data/waymo/         # 意图点文件（镜像内置）
│   └── ...
├── data/                   # 数据目录（挂载）
│   ├── processed_scenarios_testing_B1_part/       # 大测试集
│   └── processed_scenarios_testB1_part_infos.pkl   # 大测试集摘要
├── model/                  # 模型目录（挂载）
│   └── best_model_ema.pth                      # 训练好的模型权重
└── output/                 # 输出目录（挂载）
```

### 3.3 一行指令对应一个需求

#### 需求1：模型推理（生成 result.pkl）

```bash
docker run --gpus all \
    -v /host/path/data:/workspace/data \
    -v /host/path/model:/workspace/model \
    -v /host/path/output:/workspace/output \
    mtr-voyah-submission
```

输出：`/workspace/output/waymo/mtr_voyah_data/submission/test/default/result.pkl`

#### 需求2：模型训练

```bash
docker run --gpus all \
    -v /host/path/data:/workspace/data \
    -v /host/path/output:/workspace/output \
    mtr-voyah-submission \
    python3 tools/train.py \
        --cfg_file cfgs/waymo/mtr_voyah_data.yaml \
        --batch_size 64 \
        --epochs 50 \
        --extra_tag training \
        --fix_random_seed \
        --workers 8 \
        --set DATA_CONFIG.DATA_ROOT /workspace/data
```

#### 需求3：结果验证

```bash
docker run --gpus all \
    -v /host/path/data:/workspace/data \
    -v /host/path/output:/workspace/output \
    mtr-voyah-submission \
    python3 tools/verify_result.py \
        --result_pkl /workspace/output/waymo/mtr_voyah_data/submission/test/default/result.pkl
```

#### 需求4：性能评估（评估 minADE/minFDE/mAP）

```bash
docker run --gpus all \
    -v /host/path/data:/workspace/data \
    -v /host/path/output:/workspace/output \
    mtr-voyah-submission \
    bash -c "cd tools/eval_scripts && python get_gt_data.py && python eval_gt_and_pred.py"
```

#### 需求5：单条结果推理（单场景测试）

```bash
docker run --gpus all \
    -v /host/path/data:/workspace/data \
    -v /host/path/model:/workspace/model \
    -v /host/path/output:/workspace/output \
    mtr-voyah-submission \
    python3 tools/test.py \
        --cfg_file cfgs/waymo/mtr_voyah_data.yaml \
        --ckpt /workspace/model/best_model_ema.pth \
        --extra_tag smoke_test \
        --batch_size 32 \
        --workers 8 \
        --save_to_file \
        --set DATA_CONFIG.DATA_ROOT /workspace/data \
              DATA_CONFIG.SPLIT_DIR.test processed_scenarios_testing_A_part \
              DATA_CONFIG.INFO_FILE.test processed_scenarios_testA_part_infos.pkl
```

## 四、数据挂载路径

| 挂载点 | 容器路径 | 内容 |
|--------|---------|------|
| 数据 | `/workspace/data/` | 测试集 pkl 文件 |
| 模型 | `/workspace/model/best_model_ema.pth` | 训练好的权重 |
| 输出 | `/workspace/output/` | result.pkl 输出 |

## 五、输出格式

`result.pkl` 为 `list[list[dict]]` 结构：

| 字段 | 类型 | 说明 |
|------|------|------|
| scenario_id | str | 场景ID |
| pred_trajs | np.ndarray (6, 80, 2) | 6模态 × 80帧 × (x, y) |
| pred_scores | np.ndarray (6,) | 置信度分数 (sum ≈ 1.0) |
| object_id | int | 物体ID |
| object_type | str | TYPE_VEHICLE / TYPE_PEDESTRIAN / TYPE_CYCLIST |
| track_index_to_predict | int | 预测目标索引 |
