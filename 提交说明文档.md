# MTR 轨迹预测 — 赛事提交说明文档

## 一、赛题信息

| 项目 | 内容 |
|------|------|
| 赛题名称 | 目标车辆轨迹预测算法 |
| 赛道 | B赛道 |
| 关键指标 | 平均位移误差 (minADE)、最终位移误差 (minFDE)、mAP |

## 二、提交内容清单

| 文件/目录 | 说明 |
|-----------|------|
| `mtr_voyah_submission.tar.gz` | 完整可运行的 Docker 镜像 Tar 包（唯一复现源） |
| `提交说明文档.md` | 一行指令对应一个需求的操作说明 |
| `算法说明文档.md` | 算法设计、先进性、创新点 |
| `自测结果.md` | 训练日志、自测指标 |
| `演示图片/` | eval + test 演示图片（BEV 可视化） |
| `model/best_model.pth` | 训练好的模型权重 |
| `output/result.pkl` | 大测试集推理结果 |
| `Dockerfile` | Docker 镜像构建脚本 |
| `MTR/` | 完整算法代码 |

## 三、Docker 镜像使用说明

### 3.1 镜像加载

```bash
# 加载 Docker 镜像
docker load -i mtr_voyah_submission.tar.gz

# 查看镜像
docker images | grep mtr
```

### 3.2 容器内目录结构

镜像内置了代码、环境和模型权重，只有数据集通过 volume 挂载：

```
/workspace/
├── MTR/                        # 算法代码（镜像内置）
│   ├── tools/                  # 训练/推理/评估脚本
│   ├── mtr/                    # 模型代码
│   ├── data/waymo/             # 意图点文件（镜像内置）
│   └── cfgs/                   # 配置文件
├── model/
│   └── best_model.pth         # 模型权重（镜像内置）
├── docker_entrypoint.sh        # 容器启动入口脚本（镜像内置）
└── conda/envs/mtr/             # Python 环境（镜像内置）
```

### 3.3 挂载约定

| 挂载点 | 容器路径 | 内容 |
|--------|---------|------|
| 数据集 | `/mnt/data` | 原始测试集数据 |
| 输出 | `/mnt/output` | 推理结果 `result.pkl` 输出目录 |

数据集目录结构：

```
/mnt/data/
├── processed_scenarios_testing_B1_part/
└── processed_scenarios_testB1_part_infos.pkl
```

### 3.4 一行指令对应一个需求

#### 需求1：模型推理（生成 result.pkl）

```bash
docker run --gpus all --rm --shm-size=8g \
    -v /host/path/data:/mnt/data \
    -v /host/path/output:/mnt/output \
    mtr-voyah-submission
```

容器启动后自动执行推理，结果输出到 `/mnt/output/result.pkl`。

#### 需求2：模型训练

```bash
docker run --gpus all --rm --shm-size=8g \
    -v /host/path/data:/mnt/data \
    -v /host/path/output:/mnt/output \
    --entrypoint /bin/bash \
    mtr-voyah-submission \
    -c "cd /workspace/MTR/tools && python train.py \
        --cfg_file cfgs/waymo/mtr_voyah_data.yaml \
        --batch_size 64 \
        --epochs 50 \
        --extra_tag training \
        --fix_random_seed \
        --workers 8"
```

#### 需求3：结果验证

```bash
docker run --gpus all --rm --shm-size=8g \
    -v /host/path/data:/mnt/data \
    -v /host/path/output:/mnt/output \
    --entrypoint /bin/bash \
    mtr-voyah-submission \
    -c "cd /workspace/MTR/tools && python verify_result.py \
        --result_pkl /mnt/output/result.pkl"
```

#### 需求4：性能测速

```bash
# 测试推理速度：统计单 batch 平均推理耗时
docker run --gpus all --rm --shm-size=8g \
    -v /host/path/data:/mnt/data \
    -v /host/path/output:/mnt/output \
    --entrypoint /bin/bash \
    mtr-voyah-submission \
    -c "cd /workspace/MTR/tools && python benchmark_speed.py \
        --cfg_file cfgs/waymo/mtr_voyah_data.yaml \
        --ckpt /workspace/model/best_model.pth \
        --num_runs 10"
```

#### 需求5：性能评估（评估 minADE/minFDE/mAP）

```bash
# Step 1: 生成 GT 数据
docker run --gpus all --rm --shm-size=8g \
    -v /host/path/data:/mnt/data \
    -v /host/path/output:/mnt/output \
    --entrypoint /bin/bash \
    mtr-voyah-submission \
    -c "cd /workspace/MTR/tools/eval_scripts && python get_gt_data.py \
        --processed_dir /mnt/data/processed_scenarios_validation \
        --output_file /mnt/output/gt_data.pkl"

# Step 2: 评估（分别评估 3s/5s/8s）
docker run --gpus all --rm --shm-size=8g \
    -v /host/path/data:/mnt/data \
    -v /host/path/output:/mnt/output \
    --entrypoint /bin/bash \
    mtr-voyah-submission \
    -c "cd /workspace/MTR/tools/eval_scripts && python eval_gt_and_pred.py \
        --pred_file /mnt/output/result.pkl \
        --gt_file /mnt/output/gt_data.pkl \
        --eval_second 8"
```

#### 需求6：单条结果推理（单场景测试）

```bash
docker run --gpus all --rm --shm-size=8g \
    -v /host/path/data:/mnt/data \
    -v /host/path/output:/mnt/output \
    --entrypoint /bin/bash \
    mtr-voyah-submission \
    -c "cd /workspace/MTR/tools && python test.py \
        --cfg_file cfgs/waymo/mtr_voyah_data.yaml \
        --ckpt /workspace/model/best_model.pth \
        --extra_tag smoke_test \
        --batch_size 32 \
        --workers 8 \
        --save_to_file \
        --set DATA_CONFIG.SPLIT_DIR.test processed_scenarios_testing_A_part \
              DATA_CONFIG.INFO_FILE.test processed_scenarios_testA_part_infos.pkl"
```

## 四、输出格式

`result.pkl` 为 `list[list[dict]]` 结构：

| 字段 | 类型 | 说明 |
|------|------|------|
| scenario_id | str | 场景ID |
| pred_trajs | np.ndarray (6, 80, 2) | 6模态 × 80帧 × (x, y) |
| pred_scores | np.ndarray (6,) | 置信度分数 (sum ≈ 1.0) |
| object_id | int | 物体ID |
| object_type | str | TYPE_VEHICLE / TYPE_PEDESTRIAN / TYPE_CYCLIST |
| track_index_to_predict | int | 预测目标索引 |

## 五、镜像构建（可选）

如需重新构建镜像：

```bash
# 1. 打包 conda 环境
bash pack_env.sh

# 2. 构建镜像 + 导出 tar 包
bash build_docker.sh
```

镜像基于 `nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu22.04`，Python 环境通过 `conda-pack` 离线打包，构建时无需网络。

## 六、打包提交（可选）

如需重新打包提交材料：

```bash
bash pack_submission.sh
```

生成 `/tmp/mtr_voyah_submission_pack.tar.gz`，包含镜像、文档、演示图片、模型、代码等全部提交材料。
